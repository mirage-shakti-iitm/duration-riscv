(library
  (name duration)
  (public_name duration)
  (modules duration)
  (ocamlopt_flags (-default-compartment-id 10 -function-sections)))

(test
  (name tests)
  (modules tests)
  (libraries alcotest duration))
