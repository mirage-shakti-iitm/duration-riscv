(library
  (name duration)
  (public_name duration)
  (modules duration)
  (ocamlopt_flags (-default-compartment-id 18 -function-sections)))

(test
  (name tests)
  (modules tests)
  (libraries alcotest duration))
