(library
  (name duration)
  (public_name duration)
  (modules duration duration_cap_tee)
  (ocamlopt_flags (-S -function-sections)))

(test
  (name tests)
  (modules tests)
  (libraries alcotest duration))
